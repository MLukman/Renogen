<table class="ui striped very compact table">
	<thead>
		<tr>
			<th class="four wide">Branch</th>
			<th class="four wide">Modules</th>
			<th>Remark</th>
		</tr>
	</thead>
	<tbody>
		{% for row in data %}
			<tr>
				<td class="top aligned">
					{{ r.prettyDisplay(row.branch) }}
				</td>
				<td class="top aligned">
					{{ r.prettyDisplay(row.modules) }}
				</td>
				<td class="top aligned">
					{{ r.prettyDisplay(row.remark) }}
				</td>
			</tr>
		{% endfor %}
	</tbody>
</table>

